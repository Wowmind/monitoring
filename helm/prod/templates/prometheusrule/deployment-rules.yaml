{{- if and .Values.alerts.prometheusRules.enabled .Values.alerts.deployment.enabled }}
kind: PrometheusRule
metadata:
  name: {{ include "fullname" . }}-deployment-alerts
  namespace: monitoring
  labels:
    release: prometheus
spec:
  groups:
  - name: deployment.rules
    rules:
    {{- range .Values.deployment.alerts }}
      - alert: {{ .name }}
        expr: {{ .expr | quote }}
        for: {{ .for }}
        labels:
          severity: {{ .severity }}
        annotations:
          summary: {{ .summary | quote }}
          description: {{ .description | quote }}
          impact: {{ .impact | quote }}
          action: {{ .action | quote }}
    {{- end }}
{{- end }}
